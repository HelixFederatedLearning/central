import React from "react";

export default function Audit() {
  // TODO: list audit events once backend route is ready
  return (
    <div className="page">
      <h2>Audit Logs</h2>
      <div className="card">
        <div className="muted">Immutable audit events will be listed here with filters & export.</div>
      </div>
    </div>
  );
}
